import { user } from '@/api/user';
import { auth } from '@/api/auth';
import { record } from '@/api/record';
import { category } from '@/api/category';
import { currency } from '@/api/currency';

export default {
  user, auth, record, category, currency,
};
